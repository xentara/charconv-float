This library contains C++17 std::to_char and std::from_char for floating point
types (float, double, and long double). These meant as substitutes for these
functions for libstdc++ from gcc-10 and earlier, where they are missing. The
implementations are simply backported from gcc-11.
